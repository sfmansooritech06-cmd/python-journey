
Q.1 Write a Python program to calculate the length of a string.

st = input("Enter a string: ")
print(len(st))

Q.2 Write a Python program to count the number of characters (character frequency) in a string?

str = input("Enter the string :")
char = {}
for i in str:
    if i in char:
        char[i] += 1
    else:
        char[i] = 1
print(char)


3. Write a Python program to get a string made of the first 2 and last 2 characters of a given string. If the string length is less than 2, return the empty string instead.

st = input("Enter a string: ")
if len(st) < 2:
    print("")
else:
    print(st[:2] + st[-2:])


Q. 4 Write a Python program to get a string from a given string where all occurrences of its first char have been changed to '$', except the first char itself.

st = input("Enter the string:")
for char in st:
    if char == st[0]:
        st = st.replace(char, '$')
print (st)


5. Write a Python program to get a single string from two given strings, separated by a space and swap the first two characters of each string.

st1 = input("Enter the first string: ")
st2 = input("Enter the second string: ")
st3 = st1.replace(st1[:2], st2[:2])
st4 = st2.replace(st2[:2], st1[:2])
print(st3 + " " + st4)

