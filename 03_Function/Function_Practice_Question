# Print the greet with name


def greet(greet,name):
    print(f'{greet}  {name}')
greet(greet = input("Enter the greeting :"),name = input("Enter the name :"))


def power(base, exponent=2):
    return base ** exponent


2.Write a function to calculate the power of a number with default exponent as 2


result = power(int(input("Enter thee  number:")))
print(result)



3.Write a function that takes keyword arguments and prints a greeting.

def greet(name, message="Hello"):
    print(f"{message}, {name}!")


4.Write a recursive function to calculate the factorial of a number.


greet(name=(input("Enter the name:")), message=(input("Enter your message:")))


4.Write a recursive function to calculate the factorial of a number.

def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n-1)

result

6.Write a Python program to sort a list of tuples based on the second element using a lambda function.

data = [(1, 4), (3, 1), (5, 9), (2, 7)]
data.sort(key=lambda x: x[1])

print(data)




7.Write a closure function that returns a function to multiply by a given factor.


def multiplier(factor):
    def multiply_by_factor(number):
        return number * factor
    return multiply_by_factor

multiply_by_5 = multiplier(5)

print(multiply_by_5(10))


8.Write a decorator that logs the arguments and return value of a function.

def logger(func):
    def wrapper(*args, **kwargs):
        print(f"Arguments: {args}, {kwargs}")
        result = func(*args, **kwargs)
        print(f"Return value: {result}")
        return result
    return wrapper
@logger
def add(a, b):
    return a + b



9.Write a higher-order function that applies a function to every element in a list.

def apply_function(data, func):
    return [func(x) for x in data]

result = apply_function([1, 2, 3, 4], lambda x: x**2)
print(result)

add(5, 3)


10.Write a function that takes a variable number of arguments and returns their sum.

def sum_all(*args):
    return sum(args)

result = sum_all(1, 2, 3, 4, 5)
print(result)

