# Print the greet with name


def greet(greet,name):
    print(f'{greet}  {name}')
greet(greet = input("Enter the greeting :"),name = input("Enter the name :"))


def power(base, exponent=2):
    return base ** exponent


2.Write a function to calculate the power of a number with default exponent as 2


result = power(int(input("Enter thee  number:")))
print(result)



3.Write a function that takes keyword arguments and prints a greeting.

def greet(name, message="Hello"):
    print(f"{message}, {name}!")


4.Write a recursive function to calculate the factorial of a number.


greet(name=(input("Enter the name:")), message=(input("Enter your message:")))

def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n-1)

result
